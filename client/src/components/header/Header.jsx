import { StyledHeader, StyledTitle } from './header.styles';

const Header = () => {
	return (
		<StyledHeader>
			<StyledTitle>Users Dashborad</StyledTitle>
		</StyledHeader>
	);
};

export default Header;
